#include <stdio.h>
#include <stdlib.h>

int main()
{   int n ;
    printf("Bir sayi girin:\n");
    scanf("%d",&n);
    printf("%d",fib(n));
    return 0;
}
 int fib(int sayi){
 if (sayi==1|| sayi==0)
    return sayi;
 else
    return fib(sayi-1)+fib(sayi-2);
 }
